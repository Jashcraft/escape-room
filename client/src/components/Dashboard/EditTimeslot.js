import * as React from 'react';
import Typography from '../onePirate/modules/components/Typography';

export default function EditTimeslot () {
  
  return (
  <Typography>EditTimeslots</Typography>
  )
}